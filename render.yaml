services:
  - type: web
    name: chat-backend
    env: python
    plan: starter
    region: oregon
    buildCommand: pip install -r requirements.txt && cd frontend && npm ci && npm run build && cd ..
    startCommand: gunicorn -w 1 -k eventlet backend.app:app
    autoDeploy: true
