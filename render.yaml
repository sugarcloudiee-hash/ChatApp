services:
  - type: web
    name: chat-backend
    runtime: docker
    region: oregon
    plan: starter
    autoDeploy: true
    envVars:
      - key: PORT
        value: 5050
      - key: NODE_ENV
        value: production
